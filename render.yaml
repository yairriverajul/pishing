services:
  - type: web
    name: phish‑app
    env: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    # Render will bind the container to $PORT automatically; Flask must listen on 0.0.0.0.
    # No need to hard‑code the port; the container will get the env var.
    envVars:
      - key: SMTP_USER
        value: yrivera@americana.edu.co
      - key: SMTP_APP_PASS
        value: gwmgandgpaaqgafjsr
